#include <iostream>

using namespace std;

int main()
{
	int one = 1;
	int two = 2;
	// 정수 변수 one과 two를 선언하고 1과2로 초기화
	int sum = one + two; 
	// one, two 변수에 있는 값을 더하여 sum에 저장한다

	cout << "1 + 2 = " << sum << endl;
	// 1 + 2 = 를 출력한 후, sum 변수에 저장된 값을 출력

	return 0;
}
